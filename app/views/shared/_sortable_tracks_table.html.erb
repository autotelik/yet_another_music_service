<table class="table table-striped table-dark">

  <tbody class="sortable" id="target-for-track-drop-insertion-point-<%= album.id %>">  <!-- sortable target and insertion point -->

  <% album.album_tracks.order(:sort).each do |at| %>
    <%= render partial: 'tracks/sortable_tracks_row', locals: { album_track: at } %>
  <% end %>

  </tbody>
</table>
